import { defineConfig } from 'vite';

// For project site: username.github.io/quiz-cv → use base: '/quiz-cv/'
// For user site: username.github.io → use base: '/'
export default defineConfig({
  base: '/quiz-cv/',
});
